import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Dashboard/AnalysisDetail/README" />

# Analysis Detail Decision Log

This page is the canonical decision history for dashboard analysis-detail presentation choices.

Use this together with:
- `Dashboard/AnalysisDetail/DecisionMatrix` for quick tradeoff comparison
- `Dashboard/AnalysisDetail/ProgressPresentation` for visual validation

## Current Baseline

- Status: `Approved`
- Chosen variant: `DashboardRichMetadata`
- Last updated: `2026-03-08`
- Governance sync: `2026-03-08` (structured annex alignment)
- Governance sync reference: `D-08-full-governance-pass`
- Owner: `Frontend / UX`

Current approved direction:
- Keep dashboard-native detail layout (status capsule, progress stepper, event timeline, metadata cards).
- Render structured annex from `result_json.report` (`Nalazi`, `Vremenska crta`, `Konflikti`, `Otvorena pitanja`) with robust empty states when structured payload exists.
- Keep narrative markdown compatibility as fallback.

## Variant Status Board

| Variant | Status | Notes |
| --- | --- | --- |
| SubtleRailDefault | Candidate | Balanced default; progress informative but secondary |
| QuietFullStack | Candidate | Full processing visibility; higher density |
| SignalLine | Candidate | Most minimal active surface |
| ContextStrip | Candidate | Lightweight contextual orientation |
| RightRailMonitor | Candidate | Best for long result reading (desktop) |
| MilestoneDots | Candidate | Coarse milestones, low noise |
| ChronicleFirst | Candidate | Timeline-first interpretation |
| TerminalCompression | Candidate | Compresses progress after terminal states |
| DashboardRichMetadata | Approved | Dashboard baseline + metadata cards + structured annex sections with narrative fallback |
| DashboardRichMetadataCompact | Candidate | Compact version of richer metadata cards |

## Review Checklist

For each review round, validate:
1. Running + rich events: scanability and noise level
2. Running + sparse events: confidence without over-chrome
3. Completed + result: result readability takes priority
4. Failed + escalation: error salience without full-page alarm state
5. Mobile layout: no clipped controls, no unusable density

## Implementation Note

- `ID objave` in current Storybook metadata variants is a UI-derived value from `detailLink` (last URL segment).
- Candidate backend follow-up: derive and expose a canonical `entry_display_id` (or similar) so all clients render the same stable ID.

## How to Record Decisions

Every UI decision PR that changes analysis-detail direction must:
1. Update status in Storybook docs/matrix
2. Add a new history row below
3. Include PR/reference link

## Decision History

| Date | Change | Status Transition | Why | PR / Reference |
| --- | --- | --- | --- | --- |
| 2026-02-20 | Established Storybook-first variant exploration and governance docs | N/A | Make decisions explicit and trackable across iterations | This session |
| 2026-02-27 | Added metadata-module exploration variants (`DashboardRichMetadata`, `DashboardRichMetadataCompact`) plus dedicated `MetadataModules` stories | N/A | Validate “dashboard but richer” direction before patching spec/tickets | This session |
| 2026-03-08 | Promoted `DashboardRichMetadata` to approved baseline and aligned production behavior with structured annex rendering (`findings/timeline/conflicts/open_questions`) + empty-state resilience + narrative fallback | `Candidate -> Approved` (`DashboardRichMetadata`) | Close prototype/implementation gap and establish canonical analysis-detail behavior for typed-query and stage-parity release guardrails | This PR |
| 2026-08-10 | Removed auth-gated rendering from analysis-detail (and all dashboard) surfaces; detail page now renders directly with backend-provided `query.type`, no client-side login state or token plumbing | N/A (infrastructure) | Single-tenant local JSON persistence replaces Supabase auth/users; presentation decisions unchanged | `chore/analysis-only-strip` |
| 2026-08-11 | Annex timeline now renders event dates + citations when present; backend `report.timeline[]` is populated from the evidence timeline (synthesizer), so the "Vremenska crta" section is no longer permanently empty on real runs | N/A (within `DashboardRichMetadata` approved baseline) | Close the spec 8.3 `report.timeline[]` conformance gap end-to-end (synthesizer -> verifier -> payload -> annex) with fixture-backed regression coverage | `chore/analysis-only-strip` |
| 2026-08-14 | Extracted the analysis coverage banner and "Ostali pronađeni predmeti" secondary-clusters section into presentational components (`AnalysisCoverageBanner`, `SecondaryClustersSection`) and added `Dashboard/AnalysisDetail/CoverageBanner` + `Dashboard/AnalysisDetail/SecondaryClusters` stories covering complete/partial/empty states | N/A (within `DashboardRichMetadata` approved baseline) | Make discovery transparency and entity-breadth surfaces testable and visually governed; single-cluster runs hide the secondary section entirely | Journo readiness plan, Track 1d / 2c |
| 2026-08-18 | Added live token-usage tracking (input/output/total tokens + call count) from Gemini `usage_metadata`, persisted on the run and surfaced via a new `AnalysisUsageSummary` presentational card with `Dashboard/AnalysisDetail/UsageSummary` stories (`Idle`/`Accumulating`/`Final`) | N/A (within `DashboardRichMetadata` approved baseline) | Make AI cost/usage transparent during a run without polluting the stepper or event timeline | Token usage tracking |
| 2026-08-21 | Added `AnalysisActivityLog` live console (`Dashboard/AnalysisDetail/ActivityLog` stories: `Empty`/`StreamingFiles`/`WithFailuresAndRetries`/`StalledNoRecentActivity`/`CompletedFullLog`): per-file and heartbeat events stream during the AI analysis stage, rendered separately from the stage timeline, collapsed by default, with a liveness badge (amber after 90 s without activity) | `Candidate` (`Dashboard/AnalysisDetail/ActivityLog`) | Eliminate multi-minute silent gaps during document analysis (91 min observed on a 123-file run); Colab-style streaming eases "is it dead?" ambiguity and heartbeats expose stalls | Live-activity slice |
| 2026-08-23 | Failure lines in `AnalysisActivityLog` now prefer the backend-classified Croatian `reason` (from file-event metadata) over the raw technical `error`, falling back to the raw message when no classification exists; hook mapping (`useAnalysisEvents`) propagates `reason` | N/A (within `Candidate` ActivityLog variant; behavior fix per AGENTS.md "raw technical messages stay in backend logs only") | Users watching a live run saw raw SDK wording ("timed out after 30000ms") next to events that already carried a friendly classified reason nobody consumed; inconsistent with the coverage banner | Code-review remediation slice (OCR batch attribution + friendly errors) |

## Feedback Template (for review comments)

Use this format when picking or adjusting a variant:

```md
Selected variant: <name>
Keep: <what works>
Soften: <what feels too strong/noisy>
Hide behind expand: <what should be secondary>
State-specific note: <running/completed/failed/mobile>
```
